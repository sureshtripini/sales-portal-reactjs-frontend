import React from 'react';

const emailactivation = () => {
    return (
        <div>
            <h1>Click on link sent to Email for activating the account</h1>
        </div>
    );
};

export default emailactivation;