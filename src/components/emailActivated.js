import React from 'react';
import LoginContainer from '../containers/logincontainer';

const emailactivated = () => {
    return (
        <div>
            <h1>Your login account is activated successfully.</h1>
            <LoginContainer />
        </div>
    );
};

export default emailactivated;